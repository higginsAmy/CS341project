var Event  = {
	var title;
	var location
	var startDate;
	var endDate;
	var startTime;
	var endTime;
	var minVol;
	var maxVol;
	var numStudents;
	var students;
	var description;
	
	function printEvent(title, location, startDate, endDate, startTime, endTime, minVol, maxVol, numStudents, description) {
		this.title = title;
		this.location = location;
		this.startDate = startDate;
		this.endDate = endDate;
		this.endTime = endTime;
		this.minVol = minVol;
		this.maxVol = maxVol;
		this.numStudents = numStudents;
		this.students = students;
		this.description = description;

	//print string of information
	title	
}